<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ثبت‌نام - ILIYA</title>
  <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Vazirmatn', Tahoma, Arial, sans-serif;
      background-color: #f7f7f7;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
      margin: 0;
      padding-top: 60px;
    }

    .logo {
      font-size: 26px;
      font-weight: 700;
      color: #000;
      margin-bottom: 20px;
    }

    .logo span {
      color: #ff9900; /* رنگ طلایی مخصوص آمازون */
    }

    .container {
      background-color: #fff;
      padding: 25px 30px;
      border: 1px solid #ddd;
      border-radius: 8px;
      width: 330px;
      text-align: right;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h4 {
      font-size: 20px;
      margin-bottom: 20px;
      color: #111;
      font-weight: 700;
    }

    label {
      display: block;
      font-size: 14px;
      margin-bottom: 6px;
      color: #111;
    }

    input {
      width: 100%;
      padding: 9px;
      border: 1px solid #a6a6a6;
      border-radius: 4px;
      font-size: 14px;
      margin-bottom: 14px;
    }

    input:focus {
      outline: 2px solid #ff9900;
      border-color: #ff9900;
    }

    button {
      width: 100%;
      background-color: #ffd814;
      border: 1px solid #fcd200;
      border-radius: 8px;
      padding: 10px;
      font-weight: 600;
      cursor: pointer;
      font-size: 15px;
      transition: all 0.2s ease;
    }

    button:hover {
      background-color: #f7ca00;
    }

    button:active {
      background-color: #e7b800;
    }

    .error {
      color: #b12704;
      font-size: 13px;
      display: none;
      margin-top: -10px;
      margin-bottom: 10px;
    }

    .footer {
      font-size: 13px;
      color: #555;
      text-align: center;
      margin-top: 20px;
    }

    .footer a {
      color: #0066c0;
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="logo">Iliya<span>.ir</span></div>

  <div class="container">
    <h4>ایجاد حساب کاربری</h4>
    <form id="signupForm">
      <label for="email">شماره یا ایمیل</label>
      <input type="email" id="email" name="email" placeholder="مثلاً iliya@gmail.com" required>

      <label for="password">رمز عبور</label>
      <input type="password" id="password" name="password" placeholder="رمز عبور خود را وارد کنید" required>

      <label for="confirm">تأیید رمز عبور</label>
      <input type="password" id="confirm" name="confirm" placeholder="رمز عبور را دوباره وارد کنید" required>

      <div class="error" id="errorText">رمز عبور و تأیید آن مطابقت ندارند ⚠️</div>

      <button type="submit">ایجاد حساب</button>
    </form>
  </div>

  <div class="footer">
    قبلاً حساب دارید؟ <a href="#">ورود به حساب</a>
  </div>

  <script>
    const form = document.getElementById('signupForm');
    const password = document.getElementById('password');
    const confirm = document.getElementById('confirm');
    const errorText = document.getElementById('errorText');

    form.addEventListener('submit', function(e) {
      if (password.value !== confirm.value) {
        e.preventDefault();
        errorText.style.display = 'block';
      } else {
        errorText.style.display = 'none';
      }
    });
  </script>
</body>
</html>
