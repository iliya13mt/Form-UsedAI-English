<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sign Up - ILIYA</title>
  <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Vazirmatn', Tahoma, Arial, sans-serif;
      background: linear-gradient(135deg, #fff8e1, #f7f7f7);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
      margin: 0;
      padding-top: 60px;
      animation: fadeInBody 0.8s ease;
    }

    @keyframes fadeInBody {
      from { opacity: 0; transform: scale(0.98); }
      to { opacity: 1; transform: scale(1); }
    }

    .logo {
      font-size: 30px;
      font-weight: 700;
      color: #111;
      margin-bottom: 25px;
      letter-spacing: 1px;
      text-transform: uppercase;
    }

    .logo span {
      color: #ff9900;
    }

    .container {
      background-color: #fff;
      padding: 35px 40px;
      border: 1px solid #eee;
      border-radius: 14px;
      width: 340px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
      text-align: left;
      animation: fadeInUp 0.8s ease;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h4 {
      font-size: 22px;
      margin-bottom: 25px;
      color: #111;
      font-weight: 700;
      text-align: center;
    }

    label {
      display: block;
      font-size: 14px;
      margin-bottom: 6px;
      color: #333;
      font-weight: 500;
    }

    input {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 14px;
      margin-bottom: 15px;
      transition: all 0.25s ease;
    }

    input:focus {
      outline: none;
      border-color: #ff9900;
      box-shadow: 0 0 8px rgba(255,153,0,0.4);
    }

    /* Check password match visually */
    input[type="password"]:valid {
      border-color: #28a745;
    }

    input[type="password"]:invalid {
      border-color: #ccc;
    }

    button {
      width: 100%;
      background-color: #ffb300;
      border: none;
      border-radius: 8px;
      padding: 12px;
      font-weight: 600;
      color: #111;
      cursor: pointer;
      font-size: 15px;
      transition: all 0.25s ease;
    }

    button:hover {
      background-color: #f7ca00;
      transform: translateY(-1px);
      box-shadow: 0 5px 12px rgba(255,153,0,0.3);
    }

    button:active {
      background-color: #e7b800;
      transform: translateY(0);
      box-shadow: none;
    }

    .footer {
      font-size: 13px;
      color: #555;
      text-align: center;
      margin-top: 25px;
    }

    .footer a {
      color: #0066c0;
      text-decoration: none;
      font-weight: 500;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    /* Loading Screen (CSS only) */
    .loading-screen {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      z-index: 9999;
      animation: fadeOut 1s ease forwards;
      animation-delay: 2s;
    }

    .loader {
      border: 4px solid #f3f3f3;
      border-top: 4px solid #ff9900;
      border-radius: 50%;
      width: 45px;
      height: 45px;
      animation: spin 1s linear infinite;
      margin-bottom: 15px;
    }

    .loading-screen p {
      color: #333;
      font-weight: 600;
      font-size: 14px;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    @keyframes fadeOut {
      to { opacity: 0; visibility: hidden; }
    }

    @media (max-width: 400px) {
      .container {
        width: 90%;
        padding: 25px 25px;
      }
      .logo {
        font-size: 26px;
      }
    }
  </style>
</head>
<body>

  <!-- Loading Screen -->
  <div class="loading-screen">
    <div class="loader"></div>
    <p>Loading ILIYA...</p>
  </div>

  <!-- Logo -->
  <div class="logo">Iliya<span>.ir</span></div>

  <!-- Form Container -->
  <div class="container">
    <h4>Create an Account</h4>
    <form>
      <label for="email">Phone or Email</label>
      <input type="email" id="email" name="email" placeholder="iliya@gmail.com" required>

      <label for="password">Password</label>
      <input type="password" id="password" name="password" placeholder="Enter your password" minlength="6" required>

      <label for="confirm">Confirm Password</label>
      <input type="password" id="confirm" name="confirm" placeholder="Re-enter your password" minlength="6" required>

      <button type="submit">Create Account</button>
    </form>
  </div>

  <!-- Footer -->
  <div class="footer">
    Already have an account? <a href="#">Sign in</a>
  </div>

</body>
</html>
